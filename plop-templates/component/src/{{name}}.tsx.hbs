'use client';

import type { JSX } from 'react';
import type { {{pascalCase name}}Props } from './{{name}}.types';

import { forwardRef } from 'react';
import { {{camelCase name}} } from '@ideasui/theme/recipes';
import { cn } from '@ideasui/utils';

export const {{pascalCase name}} = forwardRef<HTMLDivElement, {{pascalCase name}}Props>(
  (properties, reference): JSX.Element => {
    const { className, children, classNames, variant, size, color, isDisabled, ...otherProperties } = properties;

    const styles = {{camelCase name}}({ variant, size, color, isDisabled });

    return (
      <div
        ref={reference}
        className={cn(styles.base({ className }), classNames?.base)}
        data-slot="{{kebabCase name}}"
        {...otherProperties}
      >
        {children}
      </div>
    );
  },
);

{{pascalCase name}}.displayName = 'IdeasUI.{{pascalCase name}}';