# @ideasui/{{kebabCase name}}

{{description}}

## Installation ```bash # Using npm npm install @ideasui/{{kebabCase name}}

# Using yarn yarn add @ideasui/{{kebabCase name}}

# Using pnpm pnpm add @ideasui/{{kebabCase name}}
``` ## Usage ```tsx import {
{{camelCase name}}
} from '@ideasui/{{kebabCase name}}'; // Your usage example here ``` ## API
Reference ###
{{camelCase name}}() Description of what this utility does. #### Parameters -
`param1` (Type): Description - `param2` (Type, optional): Description ####
Returns (ReturnType): Description of return value ## Development ```bash # Build
the package pnpm build # Run tests pnpm test # Run in development mode with
watch pnpm dev ```