# Examples

Real-world usage patterns and implementation examples.

## Login Form

A complete login form using our form components:

```tsx
import { Button, Input, Card, CardHeader, CardContent } from '@your-org/ui'

export function LoginForm() {
  return (
    <Card className="w-full max-w-md mx-auto">
      <CardHeader>
        <h2 className="text-2xl font-bold text-center">Sign In</h2>
      </CardHeader>
      <CardContent className="space-y-4">
        <Input 
          type="email" 
          placeholder="Email address"
          required 
        />
        <Input 
          type="password" 
          placeholder="Password"
          required 
        />
        <Button className="w-full">
          Sign In
        </Button>
      </CardContent>
    </Card>
  )
}
```

## Dashboard Layout

A responsive dashboard layout:

```tsx
import { Card, CardHeader, CardContent } from '@your-org/ui'

export function Dashboard() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <Card>
        <CardHeader>
          <h3>Total Users</h3>
        </CardHeader>
        <CardContent>
          <p className="text-3xl font-bold">1,234</p>
        </CardContent>
      </Card>
      
      <Card>
        <CardHeader>
          <h3>Revenue</h3>
        </CardHeader>
        <CardContent>
          <p className="text-3xl font-bold">$12,345</p>
        </CardContent>
      </Card>
      
      <Card>
        <CardHeader>
          <h3>Growth</h3>
        </CardHeader>
        <CardContent>
          <p className="text-3xl font-bold text-green-600">+23%</p>
        </CardContent>
      </Card>
    </div>
  )
}
```