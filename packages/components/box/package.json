{
  "name": "@iui/box",
  "version": "0.0.0",
  "description": "Box component",
  "main": "./src/index.ts",
  "sideEffects": false,
  "clean-package": "../../clean-package.config.json",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts,.tsx --cache",
    "lint:fix": "eslint src --ext .ts,.tsx --fix --cache",
    "prepack": "clean-package",
    "postpack": "clean-package restore"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-dom": ">=18.0.0"
  },
  "dependencies": {
    "@iui/utils": "workspace:*",
    "tailwind-variants": "^0.1.20"
  },
  "devDependencies": {
    "@iui/eslint-config": "workspace:*",
    "@iui/jest-config": "workspace:*",
    "@iui/tsup-config": "workspace:*",
    "@iui/tsconfig": "workspace:*",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.9.3",
    "clean-package": "^2.2.0"
  }
}