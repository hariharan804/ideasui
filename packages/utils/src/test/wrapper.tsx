// Add your providers here (Redux, Router, Theme, etc.)
// eslint-disable-next-line sonarjs/function-return-type
export const Wrapper = ({ children }: { children: React.ReactNode }): React.ReactNode => {
  return children;
};
